# 文字與符號仿圖工具（ASCII/Unicode Art）產品規格草案

## 1. 目標
建立一個可上傳「參考圖片」並自動轉換為「文字＋符號」風格輸出的工具，支援：
- 指定輸出尺寸（字幅 × 行數）。
- 指定視覺風格（寫實、馬賽克、線稿等）。
- 在預覽頁面切換常見社群留言版面（Threads / Facebook / Instagram）觀感。

---

## 2. 核心使用流程（User Flow）
1. 使用者上傳圖片（JPG/PNG/WebP）。
2. 設定輸出規格：
   - 字幅（columns）
   - 行數（rows）
   - 字元集（ASCII 基礎 / Unicode 擴充 / 自訂字元）
3. 選擇風格：
   - 寫實（灰階密度映射）
   - 馬賽克（區塊化）
   - 線稿（邊緣抽取）
4. 生成結果並即時預覽。
5. 切換社群版面模板（Threads / FB / IG）查看實際貼文觀感。
6. 匯出：純文字、圖片截圖、JSON 設定檔。

---

## 3. 功能需求（MVP）

### 3.1 圖片輸入與前處理
- 檔案大小上限（例如 10MB）。
- 自動裁切模式：
  - 等比縮放置中
  - 補邊（padding）
  - 強制拉伸（不建議，僅進階）
- 色彩處理：
  - 灰階
  - 高對比增強（可選）

### 3.2 文字仿圖引擎
- 將圖片映射到指定 `columns × rows` 網格。
- 每格取樣亮度後對應字元。
- 支援「深→淺」字元梯度（例如 `@%#*+=-:. `）。
- 支援中英混合與全形字（可選）以提升局部細節。

### 3.3 風格系統
1. **寫實（Realistic）**
   - 基於灰階亮度做連續密度映射。
   - 可調參數：對比、Gamma、銳化。
2. **馬賽克（Mosaic）**
   - 大區塊量化，降低細節。
   - 可調參數：區塊大小、離散階數。
3. **線稿（Line Art）**
   - 邊緣偵測（Sobel/Canny）後轉字元。
   - 可調參數：邊緣閾值、線條粗細。

### 3.4 版面模板預覽
- 提供三種 UI 模板：
  - Threads 留言樣式
  - Facebook 留言樣式
  - Instagram 留言樣式
- 模板切換只影響「展示層」，不改動輸出文本本身。
- 可設定：字級、行高、等寬字體、背景色（淺/深色）。

### 3.5 匯出與分享
- 複製純文字到剪貼簿。
- 匯出 `.txt`。
- 匯出帶版面模板的 PNG（方便貼文）。
- 匯出 JSON（保存參數便於重現結果）。

---

## 4. 非功能需求
- 生成速度：一般圖片（1080p）在 2 秒內完成預覽（前端 WebWorker 或後端 API）。
- 響應式介面：桌機 / 平板 / 手機。
- 穩定性：對異常檔案有錯誤提示，不使畫面崩潰。
- 可重現性：同一張圖＋同參數應得到一致結果。

---

## 5. 建議技術架構

### 5.1 前端
- React + TypeScript（或 Next.js）。
- Canvas 做圖片取樣與前處理。
- Zustand/Redux 管理參數狀態。
- WebWorker 執行轉換避免阻塞 UI。

### 5.2 後端（可選）
- Node.js（Fastify/Express）或 Python（FastAPI）。
- 任務：批次匯出、高解析度渲染、儲存設定。
- 若只做本機即時工具，可先無後端。

### 5.3 模組切分
- `image-preprocess`：縮放、裁切、灰階、邊緣化。
- `char-mapper`：亮度→字元映射。
- `style-engine`：寫實/馬賽克/線稿參數。
- `layout-preview`：Threads/FB/IG 模板渲染。
- `exporter`：txt/png/json 匯出。

---

## 6. 資料模型（範例）
```json
{
  "input": {
    "imageId": "abc123",
    "width": 1080,
    "height": 1080
  },
  "output": {
    "columns": 80,
    "rows": 60,
    "charset": "@%#*+=-:. ",
    "style": "realistic",
    "text": "..."
  },
  "preview": {
    "template": "threads",
    "theme": "dark",
    "fontFamily": "monospace"
  }
}
```

---

## 7. 演算法草案（核心）
1. 將原圖縮放到 `columns × rows`。
2. 對每個格點計算亮度 `L`（0~255）。
3. 依 `L` 對應到字元梯度索引：
   - `idx = floor((1 - L/255) * (len(charset)-1))`
4. 逐列拼接為文字矩陣。
5. 套用風格後處理：
   - 寫實：對比/Gamma 微調後再映射。
   - 馬賽克：先區塊平均再映射。
   - 線稿：先邊緣濾波再映射。

---

## 8. 介面草圖（資訊架構）
- 左側：
  - 上傳圖片
  - 輸出尺寸（字幅、行數）
  - 風格與參數滑桿
  - 字元集設定
- 中間：
  - 文字結果即時預覽（等寬字）
- 右側：
  - 社群模板切換 Tabs（Threads / FB / IG）
  - 匯出按鈕（Copy / TXT / PNG / JSON）

---

## 9. 里程碑建議
1. **M1（1 週）**：完成寫實風格 + 固定字元集 + TXT 匯出。
2. **M2（1 週）**：加入馬賽克、線稿與參數面板。
3. **M3（1 週）**：完成 Threads/FB/IG 模板預覽與 PNG 匯出。
4. **M4（0.5 週）**：效能優化與錯誤處理、儲存/載入設定。

---

## 10. 驗收標準（Definition of Done）
- 可上傳圖片並在 3 種風格間切換。
- 可設定字幅 × 行數，結果會即時更新。
- 可切換 Threads/FB/IG 三種預覽模板。
- 可成功匯出 TXT 與 PNG。
- 同參數重跑結果一致。
